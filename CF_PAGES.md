# Cloudflare Pages — umrt-community

1. Cloudflare Dashboard → Pages → Create → Connect Git
2. Select `crashdummy88/umrt-community` (or this founding repo if the original stays archived)
3. Framework preset: None · Build command: (empty) · Output directory: `/`
4. Deploy tip: `https://umrt-community.pages.dev`

Keep `robots.txt` Disallow and meta `noindex,follow` until Matt opens indexing.

Customer-facing copy lives in `index.html`, `/forums/`, and `/bulletin/`. Prefer Text CTAs use `sms:+16166065277` with a founding-member body.
